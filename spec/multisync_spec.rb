RSpec.describe Multisync do
  it "has a version number" do
    expect(Multisync::VERSION).not_to be nil
  end

  it "does something useful" do
    pending "Specs missing"
    expect(false).to eq(true)
  end
end
